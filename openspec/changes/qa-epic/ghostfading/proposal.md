# Proposal: Implement Ghostfading

## Problem
The `ghostfading` feature, as described in the `navigation` spec, is not implemented.

## Proposed Solution
Implement the `ghostfading` feature, which subtly reduces the opacity of text that covers concepts the reader has encountered in other plateaus.