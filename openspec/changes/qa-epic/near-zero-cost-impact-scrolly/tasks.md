# Tasks

- [ ] Implement the `buildScrolly` function as described in the `scrolly-engine` spec.
- [ ] Create the visual states for "Near Zero Cost Impact" plateau as described in the `plateaus` spec.
- [ ] Connect the visual states to the scroll position using the `onStep` callback from the `buildScrolly` function.
- [ ] Implement the whispers and constellation finale as described in the `navigation` spec.
